# Copy to .env and adjust as needed
PORT=4000
JWT_SECRET=change-this-to-a-long-random-string
MONGODB_URI=mongodb://127.0.0.1:27017/nova

# Production only: comma-separated list of allowed client origins for CORS.
# Example: https://novapjm.netlify.app
CLIENT_ORIGIN=
